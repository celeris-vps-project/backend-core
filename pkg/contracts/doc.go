// Package contracts defines shared DTOs used for communication between
// the API controller (cmd/api) and the host node agent (cmd/agent).
// Both sides import from here — never from each other's internal packages.
package contracts
